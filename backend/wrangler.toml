name = "sugarbackend"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
# 사용자가 Cloudflare Dashboard에서 등록해야 하는 Secret 변수들:
# NEXON_API_KEY = "your-nexon-api-key"
# GEMINI_API_KEY = "your-gemini-api-key" (새로 추가됨)
